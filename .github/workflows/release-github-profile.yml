name: release-github-profile
on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - apps/github-profile/**

concurrency:
  group: ${{ github.workflow }}-${{ github.event.ref || github.head_ref || github.ref }}
  cancel-in-progress: true

jobs:
  release:
    uses: ./.github/workflows/reusable.yml
    with:
      APP_NAME: "@app/github-profile"
