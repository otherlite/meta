- eslint 和 typescript 结合：没有读取 tsconfig 配置？子目录怎么办？编译呢？类型检查呢？jest-parser？babel?
- monorepo hardLink 下如何打包 docker？
- serverless？
- webpack 增量构建的 cache 是如何实现的？
- 微前端？module-federation? vite? 所有基础库是否都通过 MF 方式以提升构建速度?

- 了解 emp 架构、umi 架构、create-react-app、nextjs、docusaurus
- 项目初始化
- typescript config、eslint config、jest config
- 流水、docker
- qiankun + 模块 mf 化
  - MF 优化
    - 依赖 import 写法和 package.json 中定义保持一致（好处是 ts 定义保留）
    - 通过编译脚手架替换引用路径为 package，并增加 federation {remote-runtime:https://cdn.com/remote}（remote-runtime为公共模块中心，未支持remote的package可以通过exclude选项排除）
    - package 每次编译都产生不同的 remoteEntryXXXVersion.js 产物，在 magic 平台同步部署到https://cdn.com/remote/package，达到不同package引用remote地址一致，本地开发代理remoteEntry即可
  - MF 疑惑点：APP1 为 Host，APP2 位 Remote，在路由 APP1 时，Host 消费 Remote 中的 Button，此时路由切换到 APP2，会重复加载 Remote 的 Button 么？
