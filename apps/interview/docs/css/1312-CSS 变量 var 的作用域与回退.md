# 题目名
CSS 变量（`var()`）的作用域与回退

## 题目描述
CSS 自定义属性的作用域规则是什么？如何做回退值？它与预处理器变量（Sass/Less）有什么本质差异？

## 题目答案
- CSS 变量参与运行时计算，受级联与继承影响。
- `var(--x, fallback)` 可提供回退。
- 与预处理变量不同：预处理在构建期展开，CSS 变量可在运行时随主题/容器变化。

