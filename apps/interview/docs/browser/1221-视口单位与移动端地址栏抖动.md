# 题目名
视口单位与移动端地址栏抖动

## 题目描述
为什么 `100vh` 在移动端有时会出现抖动/遮挡？有哪些解决方案（动态视口单位等）？

## 题目答案
- 移动端浏览器地址栏显示/隐藏会改变可视区域，传统 vh 基于布局视口导致不一致。
- 方案：使用新的动态视口单位（如 `dvh/svh/lvh`，看兼容），或用 JS 计算真实高度写入 CSS 变量。
- 结合安全区 `env(safe-area-inset-*)` 处理刘海屏。

