# 题目名
资源预加载：`preload` / `prefetch` / `preconnect`

## 题目描述
三者分别解决什么问题？对性能指标（FCP/LCP）有什么影响？使用不当会有什么副作用？

## 题目答案
- `preconnect`：提前建立连接（DNS/TCP/TLS），降低后续请求延迟。
- `preload`：声明关键资源高优先级下载（首屏关键 CSS/字体/大图）。
- `prefetch`：低优先级预取未来可能用到的资源（下个路由）。
- 副作用：滥用会抢带宽、降低关键资源命中率；需要按关键路径谨慎配置。

