# 题目名
跨标签页通信方案对比

## 题目描述
同站点下标签页之间如何通信？对比 `BroadcastChannel`、`localStorage` 事件、`SharedWorker`、`postMessage`。

## 题目答案
- BroadcastChannel：简单直接，站点内广播。
- localStorage 事件：兼容性好但语义绕，需要写入触发。
- SharedWorker：可共享连接与状态，但实现复杂。
- postMessage：适用于 iframe/跨窗口通信，需校验 origin。

