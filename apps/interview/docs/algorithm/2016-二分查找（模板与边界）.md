# 题目名
二分查找（模板与边界）

## 题目描述
在有序数组中查找目标值；并说明“找左边界/右边界”模板。

## 题目答案
基础：`l=0,r=n-1`，循环 `l<=r`，`mid=l+(r-l)//2`。

找左边界：
- 当 `nums[mid] >= target` 时收缩 `r=mid-1`，否则 `l=mid+1`；最终检查 `l`。

找右边界：
- 当 `nums[mid] <= target` 时扩展 `l=mid+1`，否则 `r=mid-1`；最终检查 `r`。

