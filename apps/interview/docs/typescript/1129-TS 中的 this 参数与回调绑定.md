# 题目名
TS 中的 `this` 参数与回调绑定

## 题目描述
TS 里如何给函数声明一个 `this` 参数（不占运行时参数位）？它能解决什么问题？

## 题目答案
- 语法：`function fn(this: SomeType, ...args) {}`。
- 用途：约束调用方必须以正确的 this 上下文调用，防止把需要 this 的方法当普通回调传递。
- 对类方法/回调丢失 this 的问题，可用 `this` 参数 + `bind` 或箭头函数处理。

