# 题目名
品牌类型（Branding）防止语义误用

## 题目描述
结构化类型会导致“语义不同但结构相同”也能互相赋值。如何用品牌类型避免把 `UserId` 当成 `OrderId`？

## 题目答案
- 思路：在类型上增加一个不可伪造的标记字段（通常用 `unique symbol`）。
- 让不同语义的值在类型层面不可互换。
- 适用于：ID、货币、单位（ms/s）、经纬度等容易混用的领域值。

