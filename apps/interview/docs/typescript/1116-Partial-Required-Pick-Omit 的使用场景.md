# 题目名
`Partial/Required/Pick/Omit` 的使用场景

## 题目描述
这几个工具类型分别解决什么问题？在接口演进与表单场景下如何用得更安全？

## 题目答案
- `Partial<T>`：全部可选，适合 patch/update 入参（但要配合运行时校验）。
- `Required<T>`：全部必填，适合内部已校验后的强约束。
- `Pick<T, K>`：挑选字段。
- `Omit<T, K>`：排除字段（常用于去掉后端字段或内部字段）。

