# 题目名
索引签名与字面量键的约束

## 题目描述
索引签名 `[key: string]: ...` 与显式字段并存时有哪些规则？如何用字面量联合限制可用键？

## 题目答案
- 索引签名会约束所有属性的值类型，显式字段必须是其子类型。
- 想限制键集合：用 `type Keys = 'a' | 'b'`，再 `Record<Keys, V>` 或映射类型。
- 对“开放对象”谨慎使用索引签名，容易掩盖拼写错误。

