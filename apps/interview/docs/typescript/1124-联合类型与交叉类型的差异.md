# 题目名
联合类型与交叉类型的差异

## 题目描述
`A | B` 与 `A & B` 分别表示什么？为什么交叉类型可能产生 `never`？

## 题目答案
- 联合：要么是 A 要么是 B（需要收窄后使用）。
- 交叉：同时满足 A 与 B（常用于组合对象类型）。
- 若 A 与 B 在同一字段上互斥（如 `type: 'a'` 与 `type: 'b'`），交叉会变成 `never`。

