# 题目名
`unknown` 输入的运行时校验思路

## 题目描述
为什么外部输入（`JSON.parse`、接口响应）应该先用 `unknown` 接住？如何做类型守卫与运行时校验？

## 题目答案
- 外部数据不可信，直接断言成业务类型会让类型系统失效。
- 先用 `unknown`，再用 `typeof/in/Array.isArray` 等收窄。
- 复杂结构可用 schema 校验（按项目选择），把校验后的结果转换为业务类型。

