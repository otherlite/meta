# 题目名
类型与运行时的边界：类型擦除

## 题目描述
TS 类型为什么“运行时不存在”？这会导致哪些常见误区（如 `instanceof` 判断接口）？

## 题目答案
- TS 编译后类型信息被擦除，运行时代码里没有接口/类型别名。
- 运行时判断只能基于值（`typeof`、`instanceof`、属性存在性）。
- 误区：不能 `instanceof` 一个 interface；需要用类或自定义检查。

