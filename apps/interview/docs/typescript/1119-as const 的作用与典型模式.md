# 题目名
`as const` 的作用与典型模式

## 题目描述
`as const` 会带来哪些类型层面的变化？在定义常量表、路由表、事件名时如何使用？

## 题目答案
- 把字面量收窄为字面量类型，并把对象属性变为只读。
- 常用模式：
  - 常量对象 `const X = { ... } as const`，再 `type X = typeof X[keyof typeof X]`。
- 让运行时常量与类型来源保持一致，减少重复维护。

