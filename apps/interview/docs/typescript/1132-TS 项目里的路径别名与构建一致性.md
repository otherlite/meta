# 题目名
TS 项目里的路径别名与构建一致性

## 题目描述
`paths/baseUrl` 解决什么？为什么“TS 能跑但打包器不认识”很常见？如何保证别名在 TS、打包器、测试环境一致？

## 题目答案
- `paths` 只影响 TS 类型解析，不会自动影响运行时/打包器。
- 需要在 bundler、jest/vitest、eslint 等同步配置。
- 建议把别名来源收敛到一处（共享配置），避免多处漂移。

