# 题目名
字符串常见操作与性能注意点

## 题目描述
字符串拼接、截取、替换有哪些常用 API？大量拼接的性能应如何处理？

## 题目答案
- 常用：`slice/substr/substring`、`replace/replaceAll`、`split/join`、模板字符串。
- 大量拼接：优先累积到数组再 `join('')`，避免频繁生成中间字符串。
- 正则替换注意回溯风险与全局标志 `g` 的副作用（如 lastIndex）。

