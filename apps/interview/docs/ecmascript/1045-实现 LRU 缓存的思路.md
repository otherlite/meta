# 题目名
实现 LRU 缓存的思路

## 题目描述
LRU（最近最少使用）缓存如何实现？为什么常用“哈希表 + 双向链表”？

## 题目答案
- 目标：`get/set` 都接近 O(1)，并能淘汰最久未使用项。
- 哈希表：O(1) 定位节点。
- 双向链表：O(1) 移动节点到表头、O(1) 删除尾节点。
- JS 简化实现可用 `Map` 维护插入顺序（现代引擎 Map 有序），但要理解其语义。

