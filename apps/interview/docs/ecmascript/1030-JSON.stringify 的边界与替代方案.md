# 题目名
`JSON.stringify` 的边界与替代方案

## 题目描述
`JSON.stringify/parse` 会丢失哪些信息？如何处理 BigInt、循环引用、Date、undefined、函数？

## 题目答案
- 丢失：`undefined`（对象属性会被忽略）、函数、Symbol；`NaN/Infinity` 变 `null`。
- BigInt：直接 stringify 会抛错。
- Date：会变成 ISO 字符串。
- 循环引用：会抛错，需要自定义 replacer 或用结构化克隆/专用序列化库。

