# 题目名
不可变数据与结构共享

## 题目描述
为什么前端状态管理常强调不可变更新？结构共享是什么？如何在不引入巨大性能成本的情况下保持不可变？

## 题目答案
- 不可变更新让变更可追踪、便于比较（浅比较）、更利于时间旅行/撤销。
- 结构共享：只复制变更路径，其余引用复用，降低拷贝成本。
- 实践：局部拷贝（`{...obj}`/`slice`）+ 规范化数据；或使用支持结构共享的工具（按项目约束）。

