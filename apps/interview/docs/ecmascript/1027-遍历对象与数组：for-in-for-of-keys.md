# 题目名
遍历对象与数组：`for...in` / `for...of` / `keys`

## 题目描述
`for...in` 与 `for...of` 的区别是什么？遍历数组/对象分别推荐什么方式？

## 题目答案
- `for...in`：遍历可枚举键（含原型链），不适合数组（顺序/原型污染问题）。
- `for...of`：遍历可迭代对象的值（数组/Map/Set），顺序稳定。
- 对象：`Object.keys/values/entries` 更可控，再配合 `for...of`。

