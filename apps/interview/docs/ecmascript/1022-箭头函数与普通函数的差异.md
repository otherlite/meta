# 题目名
箭头函数与普通函数的差异

## 题目描述
箭头函数与普通函数在 `this`、`arguments`、`new`、原型、以及作为回调使用时有什么差异？

## 题目答案
- `this`：箭头函数是词法绑定，普通函数由调用方式决定。
- `arguments`：箭头函数没有自己的 `arguments`，可用剩余参数 `(...args)`。
- 构造：箭头函数不能 `new`，也没有 `prototype`。
- 场景：事件回调/定时器常用箭头函数避免 `this` 丢失，但不要用它当构造器。

