# 题目名
严格模式（strict mode）的影响

## 题目描述
开启严格模式后，`this`、变量声明、删除属性、重复参数名等行为有什么变化？

## 题目答案
- 默认绑定 `this`：严格模式下普通函数直接调用时 `this` 为 `undefined`。
- 禁止隐式全局变量：未声明直接赋值会报错。
- 更严格的语法校验：如函数参数重名、`with` 等。
- 删除限制：对不可配置属性的删除会报错。

