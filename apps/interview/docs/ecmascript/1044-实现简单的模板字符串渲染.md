# 题目名
实现简单的模板字符串渲染

## 题目描述
实现 `render('hi {{name}}', { name: 'A' })`，需要注意哪些点（转义、缺失字段、嵌套路径）？

## 题目答案
- 用正则匹配 `{{...}}` 占位符，并从 data 中取值替换。
- 支持嵌套路径（`a.b.c`）时要安全访问，缺失字段可回空字符串或保留原样。
- 若用于 HTML，要做输出转义防 XSS（区分文本/属性/URL 上下文）。

