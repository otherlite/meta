# 题目名
模块循环依赖的表现与解决

## 题目描述
循环依赖在 ESM 与 CJS 中分别会出现什么现象？工程上如何避免或解决？

## 题目答案
- CJS：可能拿到未初始化完的 `exports`（undefined）。
- ESM：live binding 更可控，但仍可能发生“先用后赋值”的时序问题。
- 解决：提取公共依赖到第三模块、延迟导入（动态 import）、重构依赖方向。

